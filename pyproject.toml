[project]
name = "chansecurity"
version = "1.0.0"
dependencies = ["psutil"]

[project.scripts]
chansecurity = "chansecurity.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"